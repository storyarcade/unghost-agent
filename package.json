{
  "name": "personaforge-webcontainer",
  "version": "1.0.0",
  "description": "PersonaForge - The Ultimate Persona-Driven Outreach Engine",
  "scripts": {
    "install-deps": "pip install -r requirements.txt",
    "start-backend": "python run.py",
    "start-frontend": "cd front && npm install && npm run dev",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "setup": "npm run install-deps && cd front && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}